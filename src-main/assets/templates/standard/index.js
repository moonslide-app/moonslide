RevealEditor.initialize({
    controls: true,
    progress: true,
    history: true,
    center: true,
    slideNumber: true,
    transition: 'slide',
    width: 800,
    height: 600,
    plugins: [RevealHighlight, RevealMath],
})
